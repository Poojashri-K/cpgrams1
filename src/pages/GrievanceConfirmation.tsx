import { useState } from "react"
import { Link, useSearchParams } from "react-router-dom"
import { Check, Clipboard, Star } from "lucide-react"
import { Button, Card, Textarea } from "../components/ui"
import { fetchGrievanceByReg, submitFeedback } from "../lib/services"
export function GrievanceConfirmation() { const [params] = useSearchParams(); const reg = params.get("reg") || "CPG/2026/PENDING"; const [rating, setRating] = useState(0); const [comment, setComment] = useState(""); const [sent, setSent] = useState(false); const grievance = (() => { try { return JSON.parse(sessionStorage.getItem(`cpgrams-${reg}`) || "null") } catch { return null } })(); const copy = () => navigator.clipboard?.writeText(reg); return <section className="mx-auto max-w-2xl px-4 py-12"><Card className="overflow-hidden"><div className="bg-navy p-8 text-center text-white"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-green-500"><Check aria-hidden /></div><h1 className="mt-4 text-3xl font-bold">Grievance submitted</h1><p className="mt-2 text-white/75">Keep this ID for tracking your grievance.</p><p className="mt-5 font-mono text-2xl font-bold text-saffron">{reg}</p><Button className="mt-4" variant="outline" onClick={copy}><Clipboard className="h-4 w-4" />Copy ID</Button></div><div className="space-y-4 p-6"><div className="grid gap-3 sm:grid-cols-3"><div><p className="text-xs text-muted-foreground">Date & time</p><p className="font-medium">{new Date().toLocaleString()}</p></div><div><p className="text-xs text-muted-foreground">Department</p><p className="font-medium">{grievance?.department || "Selected department"}</p></div><div><p className="text-xs text-muted-foreground">Status</p><p className="font-medium text-green-700">Submitted</p></div></div><div className="border-t pt-5"><h2 className="font-semibold">How was your filing experience?</h2>{sent ? <p className="mt-3 text-green-700">Thank you for your feedback.</p> : <><div className="mt-3 flex gap-1" aria-label="Rating">{[1,2,3,4,5].map((value) => <button type="button" key={value} onClick={() => setRating(value)} aria-label={`${value} stars`}><Star className={value <= rating ? "fill-saffron text-saffron" : "text-muted-foreground"} /></button>)}</div><Textarea className="mt-3" rows={3} value={comment} onChange={(e) => setComment(e.target.value)} placeholder="Optional comments" /><div className="mt-3 flex gap-3"><Button disabled={!rating} onClick={async () => { await submitFeedback(reg, rating, comment); setSent(true) }}>Submit Feedback</Button><Button variant="ghost" onClick={() => setSent(true)}>Skip</Button></div></>}</div><Link className="inline-flex font-semibold text-navy underline" to={`/check-status?reg=${encodeURIComponent(reg)}`}>Track grievance</Link></div></Card></section> }
export default GrievanceConfirmation
